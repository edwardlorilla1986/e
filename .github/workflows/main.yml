name: Fetch RSS Feed and Update Rep2

on:
  schedule:
    - cron: '0 0 * * *'  # This will run every hour
  workflow_dispatch:  # Manual trigger option

jobs:
  update-rss1_3:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_2.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1__3:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_3.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1__6:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_6.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1__7:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_7.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1__8:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_8.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1__4:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_5.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1_4:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_4.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss1_1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1_1.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss8_1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss8.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        


  update-rss1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests
          

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss1.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors

  update-rss2:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss2.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
  update-rss3:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss3.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss4:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss4.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}


  update-rss5:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss5.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss6:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss6.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss7:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss7.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss8:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss8.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss9:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss9.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss10:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss10.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss11:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss11.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss12:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss13.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss14:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss14.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss15:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss15.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss16:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests
      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss16.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss17:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss17.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss18:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss18.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss19:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss19.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss20:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss20.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss21:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss21.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss22:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss22.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss23:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss23.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss24:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss24.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss25:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests
      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
  update-rss26_1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss26:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss27:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss28:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}


  update-rss29:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

  update-rss30:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser
          pip install mysql-connector-python
          pip install requests

      - name: Install System Dependencies
        run: |
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer imagemagick ghostscript curl jq
  
      - name: Install Ollama
        run: |
          echo "Installing Ollama CLI..."
          curl -fsSL https://ollama.com/install.sh | sh
          ollama --version
  
      - name: Start Ollama Service
        run: |
          echo "Starting Ollama service..."
          ollama serve &
          sleep 5  # Allow time for the service to initialize
  
      - name: Verify Ollama Service
        run: |
          echo "Checking if Ollama API is running..."
          curl http://127.0.0.1:11434/ || (echo "Ollama service not responding" && exit 1)
  
      - name: Pull LLaMA Model
        run: |
          echo "Pulling the LLaMA 2 model..."
          if ! ollama pull llama3; then
            echo "Model pull failed. Please check model availability or permissions."
            exit 1
          fi

      - name: Fetch RSS feed and store data
        env:
            MYSQL_HOST: ${{ secrets.MYSQL_HOST }}  # MySQL host IP (stored in GitHub Secrets)
            MYSQL_USER: ${{ secrets.MYSQL_USER }}  # MySQL username (stored in GitHub Secrets)
            MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}  # MySQL password (stored in GitHub Secrets)
            MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        run: |
          set +e  # Allow the script to continue on errors
          python fetch_rss25.py
          if [ $? -ne 0 ]; then
            echo "Error occurred while running fetch_rss.py. Continuing with other tasks..."
          fi
          set -e  # Re-enable stopping on errors
      - name: Commit and push updated RSS data and GUIDs
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rss_data.json guids.json
          git commit -m "Update RSS feed data and GUIDs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
